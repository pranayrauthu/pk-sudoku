
export const selectGameData = ({
    game: {
        data
    }
}) => (data);

export const selectCurrentSelection = ({
    game: {
        currentSelection
    }
}) => (currentSelection);

export const selectGameStatus = ({
    game: {
        gameStatus
    }
}) => (gameStatus);
